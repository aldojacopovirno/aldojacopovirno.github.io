# Local Images Directory

This `.gitignore`d folder is for local storage only. 
To publish images, either use external hosting (like Imgur), a dedicated public repository, or a public `assets/images/` folder.
